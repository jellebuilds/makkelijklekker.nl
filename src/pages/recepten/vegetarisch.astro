---
import HubPage from '../../components/HubPage.astro';
import { getHub } from '../../lib/hubs';

const hub = getHub('vegetarisch');
if (!hub) throw new Error('Onbekende verzameling: vegetarisch');
---

<HubPage hub={hub} />
