---
import HubPage from '../../components/HubPage.astro';
import { getHub } from '../../lib/hubs';

const hub = getHub('toetjes');
if (!hub) throw new Error('Onbekende verzameling: toetjes');
---

<HubPage hub={hub} />
