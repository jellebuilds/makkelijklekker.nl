---
import HubPage from '../../components/HubPage.astro';
import { getHub } from '../../lib/hubs';

const hub = getHub('lunch');
if (!hub) throw new Error('Onbekende verzameling: lunch');
---

<HubPage hub={hub} />
