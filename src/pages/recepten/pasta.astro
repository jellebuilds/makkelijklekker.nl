---
import HubPage from '../../components/HubPage.astro';
import { getHub } from '../../lib/hubs';

const hub = getHub('pasta');
if (!hub) throw new Error('Onbekende verzameling: pasta');
---

<HubPage hub={hub} />
