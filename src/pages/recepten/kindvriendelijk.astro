---
import HubPage from '../../components/HubPage.astro';
import { getHub } from '../../lib/hubs';

const hub = getHub('kindvriendelijk');
if (!hub) throw new Error('Onbekende verzameling: kindvriendelijk');
---

<HubPage hub={hub} />
